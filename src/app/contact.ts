export class Contact {
    id: string;
    userid: string;
    title: string;
    body: string;
    descr: string;
    constructor(id: string, userid: string, title: string, body: string){
        this.id = id;
        this.userid = userid;
        this.title = title;
        this.body = body;

    }
}
